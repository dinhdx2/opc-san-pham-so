# AUDIT 2026-06-30 — Ánh-xạ spec ↔ mã-nguồn (máy kiểm sâu)

> **✅ TRẠNG-THÁI ĐÓNG (2026-06-30):** TẤT CẢ phát-hiện dưới đây đã được implement qua **5 đợt** (A1-A4, B1-B5, C1-C9, D3-D5 + gap re-audit: loop GĐ-normalize, `dang_ngon_ngu`, B4-wiring, B3-wire-skill, A1-prose). Re-audit 4 luồng độc-lập (verify + final-sweep) xác-nhận **không còn gap code-được hay bất-nhất doc↔code↔yaml↔skill**. Máy kiểm: `npm test` **164/164** · `check-impl` I1/I2/I3=162 · `check-coverage` 220/15 + AC-glossary PASS. Chi-tiết đóng: `IMPL-STATUS.md` mục "Vòng 4".

> Chạy máy kiểm độc-lập, đối-kháng: 5 luồng kiểm-toán song-song ánh-xạ TỪNG phần của 17 file spec
> `docs/design/luong-generic-v3/specs/*` ↔ `lib/*.js` · `knowledge/**/*.yaml` · `.claude/skills/**` ·
> `test/*`. Mục-tiêu: tìm phần **chưa code thành engine** (chỉ ở doc/prompt), **bug hành-vi thật**,
> và **bất-nhất / lỗi-thời** giữa các thành-phần. KHÔNG tin self-tracking (`IMPL-STATUS.md`) — tự xác-minh trên code.

## TL;DR (3 câu)
Hạ-tầng tự-kiểm đều xanh (`npm test` 146/146 · `check-impl` · `check-coverage` 220/220) NHƯNG độ-phủ là **theo claim-tag + happy-path**, che 4 vấn-đề hành-vi thật và ~15 gap/stale. Nặng nhất: **cổng G0 chặn chính vault production** (`approved_by` so-khớp cứng `=== 'CEO'`), **cổng PROMOTE nhận cả grade-C** (spec bảo bỏ), **enum stage `GĐ` (doc/vault) ≠ `GD` (code)**, và **spec 08 còn mô-tả vault đơn `vault/` trong khi hệ chạy multi-vault**. Phần lớn còn lại là gap "code-được nhưng đang ở prompt" + số-liệu doc lệch máy — đã gom thành plan 4 đợt bên dưới.

---

## A. BUG HÀNH-VI THẬT (đã xác-minh chạy code) — ƯU-TIÊN CAO NHẤT

| # | Vấn-đề | Bằng-chứng (đã chạy) | Tác-động |
|---|---|---|---|
| **A1** | **G0 so-khớp cứng `approved_by === 'CEO'` chặn vault production.** Vault thật `vaults/san-pham-so/00-Brain/telos.md:18` ghi `approved_by: "CEO (dinhdx2) — duyệt 2026-06-29 (cổng G0 MỞ)"`. `runGateG0` trả `pass:false, ly_do:"telos.approved_by != CEO"` dù CEO đã duyệt. | `lib/brain.js:122`; chạy thật: production→`pass:false`, `'CEO'` thuần→`pass:true` | **CHẶN toàn-bộ trục-dọc PHA1→4 trên vault đang dùng.** Cao nhất. |
| **A2** | **Cổng PROMOTE (`kho.them`) nhận asset `reuse_grade:'C'`.** Spec 05 §4.3 / 03d §4.3 (INV-2/INV-3): grade∈{A,B} mới vào `_index.md`, grade C → `bo_qua()`. Code chỉ kiểm `reuse_grade` non-empty (dòng 90-92), KHÔNG kiểm giá-trị. | `lib/kho.js:90-92`; chạy thật: grade C + `qua_cong_promote:true` → **ĐƯỢC GHI** vào index | Vi-phạm bất-biến C4 (luật-cứng phải ở engine); KHO ngấm rác grade-C. Thiếu cả test §8.3a. |
| **A3** | **Enum stage `GĐ1..GĐ6` (doc/vault) ≠ `GD1..GD6` (code).** `lib/brain.js:7 STAGES=['GD1'..'GD6']` (không dấu). Vault `state.md:15` + `brain-schema.md:71,75` + spec 02 ghi `GĐ` (có dấu). Nếu validator đọc `stage: GĐ1` từ state.md → `STAGES.includes('GĐ1')=false` → `STAGE_INVALID` sai. | `lib/brain.js:7` vs `vaults/san-pham-so/00-Brain/state.md:15`, `knowledge/brain-schema.md:71` | Bất-nhất enum doc↔code; lỗi tiềm-ẩn khi PHA0 ghi stage đúng dạng doc. |
| **A4** | **`HUMAN_ONLY` trả `cong:'none'` — vi-phạm bất-biến schema §3.3** ("`cong=none` chỉ khi `loai=AI_AUTO`"). | `lib/flow.js:47` (xác-minh đọc code) | Bất-nhất doc↔code; nhỏ nhưng là mâu-thuẫn trực-tiếp văn-bản spec 03c §3.3. |

## B. GAP "CODE-ĐƯỢC nhưng đang ở prompt/doc" (engine còn thiếu, đáng code-hoá)

| # | Vấn-đề | Bằng-chứng | Đề-xuất |
|---|---|---|---|
| **B1** | **Back-test cổng "TỐI-THIỂU" chỉ xét `required=true`** → mọi nhiệm-vụ-chính phòng-thủ (Lớp-2 pre-mortem) / xuyên-khía-cạnh (Lớp-3) đóng điều-kiện `required:false` bị cắt là "thừa". Phá đúng mục-tiêu chống-sót §1.4. | `lib/tier.js:102-114` | Back-test xét cả việc thủ/hậu (đóng-góp không-sụp), hoặc đánh-dấu thủ là required-ngầm. **Cần xác-minh lại bằng test trước khi sửa.** |
| **B2** | **Cổng GIAI-ĐOẠN thiếu nhánh re-debate FAIL** (03d §4.6/§8.5): `congGiaiDoan` khi đủ mục-tiêu LUÔN mở cổng + nâng stage, không có đầu-vào/nhánh "re-debate số-thật FAIL ⇒ ở-lại GĐ" (INV-8 chống mở-rộng-sớm). | `lib/loop.js:36-41`; test thiếu ca FAIL | Thêm tham-số `re_debate_pass`; FAIL → `mo_cong:false`. Thêm test §8.5. |
| **B3** | **Validator sổ `10-run-state.md` (03c §3.3 E1/E2/E3) chưa có.** Bất-biến `DONE⇒bằng-chứng≠null`, `loai=AI_AUTO⇒cong=none∧owner=AI`, resume-không-ghi-đè là code-được & là chống-bịa-bằng-chứng cốt-lõi, nhưng không có hàm `validateRunState`. | `lib/flow.js:42-48` chỉ sinh `{loai,cong,owner}` | Thêm `validateRunState(rows)` thuần + test E1/E2/E3. |
| **B4** | **`detectStage` chỉ ra 1 stage gộp, không per-đường-cong** (03a §4/§8.3 đòi `for curve in brain.curves`). DN nhiều curve (lõi GĐ6 + bet GĐ1) suy sai. | `lib/rule-engines.js:22-45`; `chuanBiPHA0` trả 1 stage | Thêm `detectStagePerCurve(curves)` + test §8.3. |
| **B5** | **Ngưỡng số hardcode trong code, không trong YAML** — `kho.tra` `w1/w2/w3=0.34/0.33/0.33`; `reuseDecision` cutoff `A≥0.8/B≥0.5`. Vi-phạm C4 ("sửa bảng generic → 1 nơi YAML"). | `lib/kho.js:134`, `lib/rule-engines.js:85`; `reuse-rubric.yaml` thiếu cột ngưỡng | Đưa trọng-số + cutoff vào YAML; engine đọc config. |
| **B6** | **`PROMOTE/cascade_sinh_lai/xet_promote` chỉ là cờ boolean, chưa nối hàm** (03d §4.1/§4.2) → không test được chuỗi PASS→PROMOTE→index hay SONG_VONG→cascade end-to-end trong engine. | `lib/loop.js:27-32,59,83-88`; không có `xetPromote`/`cascadeSinhLai` | Thêm wrapper `xetPromote(asset,index)` gọi `reuseDecision`+`kho.them` (gắn liền A2). |
| **B7** | **INV-P5 "tra-kho mỗi tầng" trong gom-ngược 1C không enforce đầu-cuối**: golden test gọi `kho.tra` thủ-công cho TẦNG 0, bỏ tầng bộ-phận/phòng/khối; không có hàm `buoc_1C` ráp. | `lib/flow.js:129-137` (`chuanBiPHA1` tĩnh); `golden-pho-ha.test.js:42` | (tùy) ráp helper gom-ngược + tra mỗi tầng, hoặc đánh-dấu prompt-only rõ. |

## C. BẤT-NHẤT / LỖI-THỜI (doc ↔ code ↔ tooling ↔ số-liệu)

| # | Vấn-đề | Bằng-chứng | Đề-xuất |
|---|---|---|---|
| **C1** | **`check-coverage.js` mồ-côi khỏi tooling gốc.** `IMPL-STATUS.md:4` + `README.md:7,9` trỏ `tools/check-coverage.js` (gốc) — file THỰC ở `docs/design/luong-generic-v3/specs/tools/check-coverage.js`; `package.json` không có script. Chạy theo README → "No such file". | `package.json:5-8`; xác-minh: chạy được từ `specs/tools/` | Thêm `"check-coverage"` npm-script trỏ đúng path + sửa IMPL-STATUS:4. |
| **C2** | **`_coverage.md` & README số claim lệch máy.** Header "220" nhưng tổng hàng "217"; per-row 00=45/04=23/05=17 thiếu +3 claim B5 (thực 46/24/18). README lẫn "220" và "217". Máy: 220 claim. | `_coverage.md:5,10,19,20,25`; `README.md:9,15`; check-coverage out "220" | Cộng +3 vào 3 hàng, sửa tổng=220. |
| **C3** | **Spec 08 mô-tả vault đơn `vault/` — lỗi-thời so multi-vault.** Spec dùng literal `vault/00-Brain/`·`vault/02-Tasks/` (7 lần), không nhắc `vaults/<slug>/` + `.vn-active-vault` + `resolveActiveVault` mà `CLAUDE.md`+`lib/vault.js`+`vaults/san-pham-so/` đang chạy. | spec 08 §3.1-4; `lib/vault.js:46,63,74` | Thêm §ghi-chú multi-vault: `vault/` = vault active resolve, fallback legacy. |
| **C4** | **`brain-schema.md` header "gồm 7 file" stale** — vault thực có 12 file 00-Brain (5 sinh-thành thêm ở v3). | `knowledge/brain-schema.md:4` vs `:57-73`; `ls vault/00-Brain`=12 | Sửa câu mở-đầu / thêm "(+5 file sinh-thành v3)". |
| **C5** | **`check-coverage` đếm `IMPL-STATUS.md`+`README.md` là "spec"** → báo 17 thay vì 15 (nguồn lệch "15 spec" của _coverage). | `specs/tools/check-coverage.js:24-28` | Loại trừ README/IMPL-STATUS khỏi `listSpecMd`, hoặc cập-nhật _coverage="17". |
| **C6** | **Spec §8 nêu fixture `*.cases.json`/`*.expected.json` không tồn-tại** (đã repoint INLINE — `IMPL-STATUS:59-60` ghi-nhận, nhưng spec gốc vẫn ghi tên file sai). | spec 03/03a/03b/03c §8 | Sửa §8 các spec: thay tên fixture bằng "INLINE trong test/*.test.js (neo claim LG-*)". |
| **C7** | **`IMPL-STATUS.md:50` overclaim "A1 đã code state-machine luồng PHA0→4"** — thực-tế chỉ `buocLuong` (pure 1-step transition) + `kiemTraThuTu`, không có struct `LuongState` (6 field) cũng không vòng `loop_gd`. Đúng kiến-trúc (skill drives loop) nhưng câu-chữ phóng-đại. | `lib/flow.js:175-219` | Hạ câu-chữ: "stepper transition; skill drives loop". |
| **C8** | **Đếm thuật-ngữ spec 00 lệch (41 vs 40).** Tiêu-đề §3.2 "(41 term)", §3.1/§4 "(40 term)", front-matter 41 `LG-G-*`. | spec 00:31 vs 83,93,115 | Chốt 41 đồng-bộ. |
| **C9** | **Test phantom `t_mapsto_chinh_phu`** — spec 04 §8/§10 liệt-kê, không tồn-tại (đang phủ bằng `t_tenkep_fnb_fixture`). | spec 04 §8,§10:212 vs `test/taxonomy.test.js` | Đổi tên test hoặc sửa spec trỏ test thật. |

## D. GAP ĐỘ-PHỦ TEST (claim "phủ test" nhưng chỉ happy-path)

| # | Claim "xanh" nhưng thiếu nhánh nghịch | Bằng-chứng |
|---|---|---|
| D1 | `LG-1.4-gate` (back-test TỐI-THIỂU): mọi cond test đều `required:true` → không lộ B1. | `test/tier.test.js` §8.2 |
| D2 | `LG-3-PHA3-promote`: thiếu ca grade-C-reject (A2) + re-debate-FAIL (B2). | `test/loop.test.js`, `test/kho.test.js` |
| D3 | `routerClass.model_tier`: test bỏ assert model cho COMPLEX/STRATEGIC; map phòng→model chỉ ở `debate.js`. | `test/flow.test.js:7-15` |
| D4 | INV-5 "trả lãi" `reuse_ratio(n+1)≥reuse_ratio(n)`: test chỉ `>0`, không so 2 curve. | `golden-pho-ha.test.js:92` |
| D5 | Bất-biến E2 (`cong⟂loai`), enum `tang`, nguồn-có-space round-trip: không có property-test. | `test/flow.test.js`, `test/kho.test.js` |

## E. ĐÃ ĐÚNG (xác-nhận — KHÔNG phải gap)
- 11 khía-cạnh + 7 khối MECE (`aspects.yaml`/`blocks.yaml`) khớp spec 04 từng hàng; `assertPartition`/`assertAspects` chặt. ✅
- 6 bảng rule-engine: mỗi bảng có 1 hàm + 1 YAML + test phủ MỌI dòng; **không dòng-bảng nào thiếu row trong YAML**. ✅
- `_index.md` đúng 13 cột; serialize/parse round-trip (trừ cạnh nguồn-có-space — C/D5). ✅
- 3-phép-thử moat/telos/brand trực-giao; golden Phở Hà 6 case PHA0→4 đầy-đủ. ✅
- I4=10 claim prose-only: xác-minh 10/10 đều là **nguyên-lý §0/§1.1 / phân-rã / HITL cổng A-B / schema-doc** — đúng kiến-trúc "generic=code · phân-rã=prompt · HITL=main-loop", KHÔNG phải gap.

---

# PLAN IMPLEMENT (4 đợt — chưa thực-thi)

> Nguyên-tắc giữ: chỉ THÊM/SỬA-cho-đúng-spec, KHÔNG phá engine debate/router/executor/HITL/grounding.
> Mỗi mục: sửa code/yaml 1 nơi → thêm test neo claim → `npm test` + `check-impl` + `check-coverage` xanh → commit.

## Đợt 1 — BUG chặn vận-hành (làm ngay, nhỏ, rủi-ro thấp)
1. **A1 G0 approved_by**: đổi `=== 'CEO'` → chuẩn-hoá/`/^CEO\b/` (chấp "CEO (tên) — duyệt…"). Test: telos production PASS; `[chờ CEO duyệt]` vẫn BLOCK. *(chạm `lib/brain.js`)*
2. **A3 enum GĐ/GD**: normalize `Đ→D` khi đọc stage (hoặc thống-nhất). Test: `stage:'GĐ4'` hợp-lệ. *(`lib/brain.js` + `lib/rule-engines.js` nơi parse stage)*
3. **A4 HUMAN_ONLY cong**: hoặc cho `cong:'none'` hợp-lệ khi `loai∈{AI_AUTO,HUMAN_ONLY}` (sửa spec 03c §3.3) **hoặc** trả `cong:'human'`. Chốt 1 hướng. *(`lib/flow.js` hoặc spec)*

## Đợt 2 — Đóng cổng an-toàn code-được (engine + test nhánh-nghịch)
4. **A2+B6 grade-gate PROMOTE**: `them()` reject `reuse_grade∉{A,B}`; thêm wrapper `xetPromote`. Test §8.3a grade-C→không-vào-index. *(`lib/kho.js`)*
5. **B2 re-debate FAIL cổng GĐ**: thêm tham-số/nhánh `re_debate_pass===false → ở-lại-GĐ`. Test §8.5. *(`lib/loop.js`)*
6. **B3 validateRunState**: hàm thuần kiểm E1/E2/E3 + test. *(`lib/flow.js` hoặc `lib/runstate.js` mới)*
7. **B1 back-test thủ/hậu**: xác-minh bằng test tái-hiện trước, rồi sửa `tier.js` xét việc không-`required` đóng-góp chống-sụp. *(`lib/tier.js` — cần thận-trọng, có thể đổi hành-vi gate)*

## Đợt 3 — Code-hoá nốt generic + bỏ hardcode
8. **B5 ngưỡng→YAML**: chuyển `w1/w2/w3` + cutoff A/B vào `reuse-rubric.yaml`/`loop-config.yaml`; engine đọc. *(`lib/kho.js`,`lib/rule-engines.js`,2 yaml)*
9. **B4 detectStagePerCurve**: hàm suy stage per-đường-cong + test §8.3. *(`lib/rule-engines.js`)*
10. **D3/D4/D5 test nhánh-nghịch**: model COMPLEX, reuse_ratio monotonic, E2 property, enum `tang`, nguồn-có-space. *(`test/*`, + sửa nhỏ `kho.js` enum `tang` + parse nguồn)*

## Đợt 4 — Dọn doc/tooling/số-liệu cho nhất-quán
11. **C1 npm script check-coverage** + sửa `IMPL-STATUS:4`, `README:7,9`.
12. **C2/C8 số claim**: `_coverage.md` 217→220 + per-row +3; spec 00 "41 term" đồng-bộ.
13. **C3 spec 08 multi-vault**: thêm §ghi-chú `vaults/<slug>/`+`resolveActiveVault`.
14. **C4 brain-schema header**, **C5 listSpecMd loại README/IMPL-STATUS**, **C6 §8 fixture repoint trong spec gốc**, **C7 hạ overclaim IMPL-STATUS:50**, **C9 test t_mapsto_chinh_phu**.

## Thứ-tự đề-xuất
Đợt 1 (3 bug, ~1 commit/bug) → Đợt 2 (an-toàn, mỗi mục 1 commit có test nhánh-nghịch) → Đợt 3 → Đợt 4 (doc, gộp ít commit).
Mỗi commit: `npm test && npm run check-impl && node docs/design/luong-generic-v3/specs/tools/check-coverage.js` đều xanh trước khi push.
